name: 'reluvia_analytics'
version: '1.0.0'
config-version: 2

profile: 'reluvia'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  reluvia_analytics:
    marts:
      +materialized: table
      +schema: analytics
